# fake_news_detection
A full-stack misinformation detection platform (React, Node/Express, Python FastAPI) with a bias-corrected TF-IDF classifier, multi-source verification (SerpAPI, NewsAPI), and URL-based article scraping via Cheerio.
